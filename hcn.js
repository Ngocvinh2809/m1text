let inputwidth;
let inputheight;
    inputwidth = prompt("Nhập chiều dài");
    inputheight = prompt("Nhập chiều rộng") ;
    let width = parseInt(inputwidth);
    let height = parseInt(inputheight);
    let tổng = width * height ;
    document.write("tổng :" + tổng);